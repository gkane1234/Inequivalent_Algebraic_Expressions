package counting_operations.Java_Version;

public class Solver {
    ExpressionList solverList;
    Solver(int numValues){
        solverList = new ExpressionDynamic(5,5,5,null).getExpressionList();
    }
}
